<!-- impeccable:product-schema 1 -->

# 문단 흐름 수리소 — Product Truth

## Product

- **Platform:** Web (static Vite + React + TypeScript)
- **Primary users:** 한국어로 학습하는 초등학교 3~6학년 학습자
- **Use context:** 교실 또는 개인 학습에서 한 번에 15~25분 정도 사용하는 문단 흐름 학습 활동
- **Product purpose:** 학습자가 문단의 중심 문장, 뒷받침 문장, 관련 없는 문장, 연결 관계를 근거와 함께 점검하도록 돕는다.
- **Positioning:** 점수·순위·경쟁 대신, 고정된 6개 미션을 차례로 수리하며 자신의 판단 근거를 남기는 학습 도구

## Product truth from the repository

- 학습 흐름은 `입장 → 문단 전체 읽기 → 중심 문장 찾기 → 문장 순서 수리 → 관련성 점검 → 연결 관계 선택 → 설명 남기기 → 학습 기록`이다.
- 미션은 6개이며 각 미션의 문장, 허용되는 순서, 관계, 피드백 근거는 `src/content/missions.ts`가 단일 출처다.
- 첫 판단과 최종 수리 결과를 비교해 보여 주지만 점수, 등수, 성취도 배지는 계산하거나 표시하지 않는다.
- 런타임 서버, 로그인, 외부 AI, 분석, 광고, 쿠키, `localStorage`·`sessionStorage` 등 브라우저 저장을 사용하지 않는다.
- 앱은 라이트 모드로 고정한다. 다크 모드나 `prefers-color-scheme: dark` 대응을 추가하지 않는다.
- 학생용 VoiceOver/TTS/내레이션/녹음 기능은 제품 범위가 아니다. 키보드 접근성, 포커스 이동, 글자 크기 조절, reduced motion은 계속 검증한다.
- 중요한 단계 진행 버튼에는 `gi-pulse` 계열의 현재 행동 강조를 사용하되, 장식이나 모든 버튼에 반복 적용하지 않는다.
- 화면의 업데이트 내역 버튼으로 앱 개발·개선 기록을 열람할 수 있어야 한다.

## Content and trust boundaries

- 미션 내용과 교육적 표현은 현재 저장소의 고정 콘텐츠를 보존한다. 리디자인 과정에서 교사 승인, 학생 효과, 접근성 인증, 출시 상태를 새로 주장하지 않는다.
- 현재 `docs/content-review.md`에 기록된 콘텐츠 검토 상태는 `pending`이며, 리디자인 후에도 교사 검토 전 상태로 둔다.
- 이미지 자산은 생성형 이미지 또는 저장소에 이미 존재하는 파일만 사용하며, 라이선스·생성 정보·검토 상태를 자산 원장에 남긴다.

## Learner and teacher commitments

- 학습자가 현재 미션, 전체 진행 위치, 다음 행동을 한눈에 이해할 수 있어야 한다.
- 문장 판단의 정답을 숨은 점수로 환산하지 않고, 왜 그렇게 수리하는지 읽을 수 있는 근거를 제공한다.
- 320px 폭에서도 가로 스크롤 없이 핵심 조작을 완료할 수 있어야 한다.
- 키보드만으로 모든 단계와 대화상자를 이용할 수 있고, 단계 전환 후 제목과 현재 작업 위치를 알 수 있어야 한다.
- reduced motion 환경에서는 펄스·스크롤 애니메이션을 줄이되, 현재 필수 행동의 의미는 텍스트와 구조로 유지한다.

## Not confirmed in this repository

- 실제 교사 수업 관찰, 학습자 사용성 인터뷰, VoiceOver 검증, 배포 후 실사용 결과는 이번 리디자인의 근거로 확인되지 않았다.
- 디자인 선호에 관한 별도 사용자 선택은 이 세션에서 제공되지 않았으므로, 저장소의 학습 목적·기능 계약·명시된 리디자인 요청을 바탕으로 시각 방향을 추론한다.

## Product principles

1. **읽기에서 근거로:** 문단을 먼저 읽고, 선택과 이유가 자연스럽게 이어진다.
2. **수리대가 보이게:** 현재 문장과 다음 조작이 작업대처럼 명확히 드러난다.
3. **실수는 재료:** 재시도와 피드백은 실패 낙인 대신 다시 살펴볼 근거를 제공한다.
4. **기록은 정직하게:** 저장·점수·승인을 가장하지 않고 이번 세션의 관찰 가능한 결과만 보여 준다.
